{
  "name": "sst-auth-svelte",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "sst build",
    "deploy": "sst deploy",
    "dev": "sst dev",
    "remove": "sst remove",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22",
    "@types/aws-lambda": "8.10.147",
    "sst": "3.9.37",
    "typescript": "^5.4.2",
    "vitest": "^1.3.1"
  },
  "pnpm": {
    "overrides": {
      "@types/cookie": "^1.0.0"
    },
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
